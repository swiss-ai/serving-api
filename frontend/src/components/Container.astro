---
import { cn } from "@lib/utils";

type Props = {
  class?: string;
};

const { class: className } = Astro.props;
---

<div class={cn("mx-auto max-w-screen-lg px-6", className)}>
  <slot />
</div>
